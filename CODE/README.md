# Progetto PAC F1 — Maven project

Struttura minima Maven creata per il progetto.

Build e test (da terminale nella cartella `CODE`):

```bash
# verificare Maven
mvn -v

# compilare e creare il pacchetto
mvn package

# eseguire i test
mvn test
```

Classe principale: `com.ivancaccamo.progetto.App`.
